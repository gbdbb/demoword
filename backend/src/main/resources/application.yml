server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
  sql:
    init:
      mode: never   # 避免每次启动清空/重灌 demo 数据；需要初始化时手动执行 init.sql
      continue-on-error: true
